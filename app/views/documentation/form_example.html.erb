
<%%= simple_form_for @user_basic, url: create_basic_examples_url, as: 'user_basic'  do |f| %>

  <%%= f.input :email, placeholder: 'Enter email' %>

  <%%= f.input :password, placeholder: 'Password' %>

  <%%= f.input :file, as: :file %>

  <%%= f.input :active, wrapper: :foundation_customizable_wrapper %>

  <%%= f.input :choices, as: :check_boxes,
    collection: [
      'Option one is this and that—be sure to include why it\'s great',
      'Option two can be something else and selecting it will deselect option one'],
      wrapper: :foundation_customizable_wrapper %>

  <%%= f.input :sex, as: :radio_buttons,
    collection: ['Male', 'Female'], wrapper: :foundation_customizable_wrapper %>

  <%%= f.button :submit %>
<%% end %>
